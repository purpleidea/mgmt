import "fmt"
import "sys"

$env = sys.env()
$m = map_lookup_optional($env, "GOPATH", "")

print "print0" {
	msg => if sys.hasenv("GOPATH") {
		fmt.printf("GOPATH is: %s", $m)
	} else {
		"GOPATH is missing!"
	},
}
