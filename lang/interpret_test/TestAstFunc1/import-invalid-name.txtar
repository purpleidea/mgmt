-- main.mcl --
# An invalid import name (eg an absolute path) must carry a source position
# byline pointing at the offending import statement.
import "/foo"
test "t" {}
-- OUTPUT --
# err: errSetScope: import `/foo` is not valid: absolute paths are not allowed: /main.mcl @ 3:1-3:14
