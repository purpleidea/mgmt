-- main.mcl --
$fn = func(
	$x,
	,
	$y,
) {
	$x + $y
}

$out = $fn("hello", "world")
test "${out}" {}
-- OUTPUT --
# err: errLexParse: parser: `syntax error: unexpected COMMA, expecting CLOSE_PAREN` @1:12
