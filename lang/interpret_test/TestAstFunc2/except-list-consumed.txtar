-- main.mcl --
import "fmt"

$src = ["a", "b", "c"]
$a = $src[0]

func b() {
	$src[42]
}

$d = func() {
	fmt.printf("%v", 42 / 0)
}

$items = [$a, b(), "c", $d()] # many of these can error
$out = fmt.printf("%v", $items)

test [$out <|> "caught"] {}
-- OUTPUT --
Vertex: test[caught]
