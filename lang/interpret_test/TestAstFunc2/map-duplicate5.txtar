-- main.mcl --
import "fmt"

$k1 = ["a", "b", "c",]

$m = {$k1 => 42, ["x", "y", "z",] => 13,} # incompatible type!

$found = $m[$k1] || 99

test [fmt.printf("value: %d", $found),] {}
-- OUTPUT --
# err: errInit: map key type []str is not hashable
