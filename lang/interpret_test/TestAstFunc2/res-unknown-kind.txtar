-- main.mcl --
# An unknown resource kind must carry a source position byline pointing at the
# offending resource statement, rather than being reported without a location.
notaresource "t" {}
-- OUTPUT --
# err: errInterpret: error building resource: cannot create resource kind `notaresource` with name `t`: no resource kind `notaresource` available: /main.mcl @ 3:1-3:20
